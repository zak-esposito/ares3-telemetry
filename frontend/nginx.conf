server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    # SPA fallback: serve index.html for client-side routes so deep links and
    # refreshes don't 404. Real asset paths are tried first.
    location / {
        try_files $uri $uri/ /index.html;
    }
}
